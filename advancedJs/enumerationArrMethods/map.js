'use strict';

// map - изменяет каждый элемент внутри массива и возвращает новый

const answers = ['IvAn', 'AnnA', 'Hello'];

const result = answers.map(answer => answer.toLowerCase());

console.log(result);
